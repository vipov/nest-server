import { PerformenceInterceptor } from './performence.interceptor';

describe('PerformenceInterceptor', () => {
  it('should be defined', () => {
    expect(new PerformenceInterceptor()).toBeDefined();
  });
});
